1. change the base url to  a new one if running the fastapi locally or
    reverse proxy it to the new url.

2. also do ```pip install -r requirements.txt``` for dependencies
3. run the ui1.py file